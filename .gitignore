# dependencies
node_modules/
node_modules

# typescript
*.tsbuildinfo

# macOS
.DS_Store

# build
dist/
.build-cache/
.build-cache

# Expo
.expo/
.expo

# Signing
android-release.keystore
*.jks
*.p8
*.p12
*.key
!test/fixtures/files/sample.key
*.mobileprovision
*.pem

# Native (generated by expo prebuild). Anchored: unanchored `ios`/`android`
# also swallowed apps/mobile/modules/*/ios, whose sources are checked in, so
# new native files in a local module silently failed to be committed.
/apps/mobile/ios/
/apps/mobile/android/

# env files
.env
.env.*
!.env.example
!.env.test

# Claude Code
# CLAUDE.md, AGENTS.md and REVIEW.md are checked in; personal notes go in CLAUDE.local.md.
CLAUDE.local.md
.claude/
.claude

# docs
docs/

