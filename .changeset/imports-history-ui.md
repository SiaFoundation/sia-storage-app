---
mobile: minor
---

The new Imports screen lists every import with plain-language per-file failure reasons, retry for failed files, and cancel or remove from the detail screen. An open photo import with nothing left to copy reads Watching instead of Importing.
