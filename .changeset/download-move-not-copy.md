---
mobile: patch
core: patch
---

Downloads now move the finished file into storage instead of copying it, halving the disk space a download needs and no longer leaving a temporary duplicate behind in the cache.
